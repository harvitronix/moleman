# Agent Guidelines

## Versioning and Changelog

- Keep `package.json` and `CHANGELOG.md` aligned with release intent.
- Use release automation for version bumps; avoid manual version changes unless requested.
- When user-visible changes are made, add an entry to `CHANGELOG.md`.
