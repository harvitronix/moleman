agents:
  codex:
    type: codex
    args:
      - "--full-auto"
    model: gpt-5-codex
    thinking: medium
    timeout: 45m
    capture: [stdout, stderr, exitCode]

  claude:
    type: claude
    model: sonnet
    timeout: 30m
    capture: [stdout, stderr, exitCode]
