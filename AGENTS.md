# Agent Guidelines

## Versioning and Changelog

- Keep `version.go` and `CHANGELOG.md` aligned with release intent.
- Use release automation for patch bumps; avoid manual version changes unless requested.
- When user-visible changes are made, add an entry to `CHANGELOG.md`.
